if( !(-d "build") ){
    mkdir "build"
}

if( !(-d "build/win") ){
    mkdir "build/win"
}

if( !(-d "build/linux") ){
    mkdir "build/linux"
}

if( !(-d "build/macosx") ){
    mkdir "build/macosx"
}
